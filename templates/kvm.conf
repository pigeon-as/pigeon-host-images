# KVM tuning for Firecracker hosts.

# Constrain kvm-pit CPU overhead — Firecracker prod-host-setup recommends this
# to limit host CPU usage from guest timer interrupts.
options kvm min_timer_period_us=200
