#!/bin/sh
# Copy tpm2_pcrextend into initrd for pigeon-verify

PREREQ=""
prereqs() { echo "$PREREQ"; }
case "$1" in prereqs) prereqs; exit 0;; esac

. /usr/share/initramfs-tools/hook-functions

if [ -x /usr/bin/tpm2_pcrextend ]; then
    copy_exec /usr/bin/tpm2_pcrextend /usr/bin
fi
